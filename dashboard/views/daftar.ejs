<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Daftar Barang | Dashboard Penjualan</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f5f6fa;
      font-family: 'Poppins', sans-serif;
    }
    header {
      background: #2c3e50;
      color: #fff;
      padding: 20px 0;
      text-align: center;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    header h1 {
      font-weight: 600;
      margin-bottom: 5px;
    }
    header p {
      color: #bdc3c7;
      margin: 0;
      font-size: 14px;
    }
    .card {
      border: none;
      border-radius: 15px;
      overflow: hidden;
    }
    .card-body {
      padding: 30px;
    }
    .table thead {
      background-color: #1abc9c;
      color: white;
    }
    .btn-success {
      background-color: #1abc9c;
      border: none;
    }
    .btn-success:hover {
      background-color: #16a085;
    }
    .btn-secondary {
      background-color: #34495e;
      border: none;
    }
    .btn-secondary:hover {
      background-color: #2c3e50;
    }
  </style>
</head>
<body>

  <header>
    <h1>Daftar Barang</h1>
    <p>Menampilkan data seluruh barang di sistem penjualan</p>
  </header>

  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-lg-10">
        <div class="card shadow-lg">
          <div class="card-body">
            <h3 class="text-center text-primary mb-4">📦 Data Barang Tersedia</h3>
            
            <table class="table table-hover align-middle text-center">
              <thead>
                <tr>
                  <th>No</th>
                  <th>Nama Barang</th>
                  <th>Harga</th>
                  <th>Stok</th>
                </tr>
              </thead>
              <tbody>
                <% products.forEach((product, idx) => { %>
                  <tr>
                    <td><%= idx + 1 %></td>
                    <td><%= product.name %></td>
                    <td>Rp <%= product.price.toLocaleString('id-ID') %></td>
                    <td><%= product.qty %></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>

            <div class="d-flex justify-content-between mt-4">
              <a href="/tambah" class="btn btn-success px-4">+ Tambah Barang</a>
              <a href="/" class="btn btn-secondary px-4">🔙 Kembali ke Dashboard</a>
            </div>

          </div>
        </div>
      </div>
    </div>
  </div>

</body>
</html>
